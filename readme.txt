To run this project, install the "live-server" npm package: "npm install live-server -g"

I've compliled the SASS using the SASS npm package: "npm install -g sass" and running "sass --watch sass/style.scss:css/style.css" in the root directory.